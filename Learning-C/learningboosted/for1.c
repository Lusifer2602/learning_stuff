#include<stdio.h>
int main(){
    int i; //declare the loop variable
    //now lets create a for loop in this
    for (i=1; i<=5; i++){
        printf("The number now is : %d\n", i);
    }

    return 0;
}

#include <stdio.h>
int main() {
        int a=10, b=123;
        int sum=a+b;
            printf("SOS ke kitne baap?!\nSOS ke %d baap.\n", sum);
            printf("We can also add variables in print statement,\nlike here a+b is %d\n", a+b);
            return 0;
}

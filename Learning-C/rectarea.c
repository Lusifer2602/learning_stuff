#include <stdio.h>
int main(){
   int  length = 32;
   int breadth = 65;
   int area=length*breadth;
   printf("Length of the Rectange is : %d\n", length);

   printf("Breadth of the Rectange is : %d\n", breadth);

   printf("Area of the Rectange is : %dsq.units\n", area);
}

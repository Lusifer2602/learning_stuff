#include <stdio.h>

int main() {
	int x=5;
	printf("Value of x is %d\n", x);
	return 0; //strings with single characters are enclosed in single quotes while the ones with more than one character are enclosed in double quotes.

} //just some changes to check linking with GitHub

/* this is a 
 multiline comment*/

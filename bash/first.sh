ls
pwd
# bash runs as an intepreter so the commands are run one by one till it runs into an error
errortext
#and here we will see that after running first two files the interpreter crashes and throws error


